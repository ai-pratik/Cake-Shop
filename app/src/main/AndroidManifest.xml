<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bakeryapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Payment_Gateway"
            android:exported="true" />
        <activity
            android:name=".PaymentGateway"
            android:exported="true" />
        <activity android:name=".Contact_us" />
        <activity android:name=".About_us" />
        <activity android:name=".Placed_order" />
        <activity android:name=".No_internet" />
        <activity android:name=".cake_animation" />
        <activity android:name=".Selected_item" />
        <activity android:name=".LoginActivity" /> <!-- <activity android:name=".homeScreen" /> -->
        <activity android:name=".RegistrationScreenActivity" />
        <activity
            android:name=".HomeScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>